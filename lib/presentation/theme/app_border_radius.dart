import 'package:flutter/painting.dart';

class AppBorderRadius {
  AppBorderRadius._();

  static const c2 = BorderRadius.all(Radius.circular(2));
  static const c4 = BorderRadius.all(Radius.circular(4));
  static const c6 = BorderRadius.all(Radius.circular(6));
  static const c8 = BorderRadius.all(Radius.circular(8));
  static const c12 = BorderRadius.all(Radius.circular(12));
}
